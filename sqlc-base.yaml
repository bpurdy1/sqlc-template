version: "2"
sql:
  - engine: "sqlite"
    schema: "schema"
    queries: "queries"
    gen:
      go:
        package: "sqlc"
        out: "sqlc"
        emit_interface: true
        emit_json_tags: true
     